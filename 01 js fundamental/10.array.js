//collection of different variable

//one way to initilize
let freindlist = ["yash", "vishal", "manish", "anskit"];
console.log(freindlist);

//second way
let ml = new Array(1991, 1992, 1993);

//find length
console.log(ml.length);

//  ================================================
("Basic operations");
//push methode :- add element at the end and return the size of arr
ml.push("ankit");

//unshift methode : add element at the front side and return the size fo arr
ml.unshift("first");

//.pop() :- remove element front the end and return the remove elememnt

//shift() :- remove element front the front side and return the remove element

// indexOf :- return the index of that element that you searching
ml.indexOf("1991");

//includes :- return true if element present in array or not
ml.includes("77");
// =====================================================
